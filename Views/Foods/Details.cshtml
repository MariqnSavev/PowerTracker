@model PowerTracker.Models.Food
@{
    ViewData["Title"] = "Детайли за храна";
}

<h2>@Model.Name</h2>

@if (!string.IsNullOrEmpty(Model.PhotoUrl))
{
    <img src="@Model.PhotoUrl" alt="@Model.Name" style="max-width: 200px;" />
}

<table class="table table-bordered mt-3">
    <tr>
        <th>Марка</th>
        <td>@Model.Brand</td>
    </tr>
    <tr>
        <th>Баркод</th>
        <td>@Model.Barcode</td>
    </tr>
    <tr>
        <th>Грамаж на порцията</th>
        <td>@Model.ServingSize г</td>
    </tr>
    <tr>
        <th>Калории (на 100г)</th>
        <td>@Model.Calories kcal</td>
    </tr>
    <tr>
        <th>Протеин (на 100г)</th>
        <td>@Model.Protein г</td>
    </tr>
    <tr>
        <th>Мазнини (на 100г)</th>
        <td>@Model.Fat г</td>
    </tr>
    <tr>
        <th>Въглехидрати (на 100г)</th>
        <td>@Model.Carbs г</td>
    </tr>
</table>

<h4>Общо за порцията (@Model.ServingSize г):</h4>
<ul>
    <li><strong>Калории:</strong> @(@Model.Calories * Model.ServingSize / 100) kcal</li>
    <li><strong>Протеин:</strong> @(@Model.Protein * Model.ServingSize / 100) г</li>
    <li><strong>Мазнини:</strong> @(@Model.Fat * Model.ServingSize / 100) г</li>
    <li><strong>Въглехидрати:</strong> @(@Model.Carbs * Model.ServingSize / 100) г</li>
</ul>
